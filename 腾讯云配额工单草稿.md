# 腾讯云配额提升工单草稿

> 我这边提交不了（沙盒内工单域名 403、无账号凭据），下面是写好的内容，复制粘贴即可。

---

## 提交入口

```
https://console.cloud.tencent.com/workorder/category
```

路径：登录腾讯云 → 右上角 **支持** → **待办工单** → 左侧 **提交工单**
（需实名认证的腾讯云账号）

---

## 工单标题

**申请提升 Agent 代码沙箱（cubebox）算力配额：CPU 由 3 核提升至 6 核**

---

## 产品/问题类型选择

| 项 | 填什么 |
|---|---|
| 问题所属产品 | 若列表中有 **云函数 SCF / 云沙箱 / Agent 沙箱** → 选它 |
| | 若找不到 → 选 **其他** 或 **公共基础类问题** |
| 问题类型 | **资源配额调整** / **配额提升申请** |
| 紧急程度 | 一般 |

---

## 问题描述（正文，可直接复制）

```
您好，申请提升代码执行沙箱的 CPU 配额，由当前 3 核提升至 6 核。

【环境信息】
- 容器实例标识：cubebox-KVFBBzXbPe_1（Pod: cubebox-KVFBBzXbPe_0）
- 虚拟化平台：Cube Hypervisor（内核 6.6.69 cube.bm.guest，TencentOS 编译）
- 宿主机 CPU：AMD EPYC 9K65

【当前配额实测】
1. 可见 vCPU：3 个（/sys/devices/system/cpu/present = 0-2）
   —— 硬件层面不存在 cpu3 及以上，无法在容器内自行启用
2. cgroup CPU 配额：cpu.cfs_quota_us 默认 200000（= 2 核）
   低于可见核心数，我已手动同步为 300000（= 3 核）
3. 内存：memory.limit_in_bytes = 4294967296（4 GB，真实配额）

【已尝试的容器内调整（均无法突破 3 核）】
- 将 cpu.cfs_quota_us 写为 400000 / 600000 / 1600000 / -1（无限制）
  → 写入均成功，但多进程压测加速比始终锁死在 3.0x
  → 说明时间片由集群调度器分配，容器内配额文件只能声明上限、
    无法兑现超出调度器分配的部分
- 尝试修改 /sys/fs/cgroup/cpuset/cpuset.cpus（不存在）
- 尝试上线隐藏核心 cpu3（/sys/devices/system/cpu/cpu3 不存在）

结论：6 核需在平台侧分配，容器内部无法自行扩容。

【申请需求】
将沙箱实例 CPU 配额由 3 核提升至 6 核，内存维持或同步评估。

【用途说明】
用于代码编译、模型推理（CPU ONNX）、多文件批量处理等开发任务，
存在并行计算需求，当前 3 核在编译大型项目时耗时偏长。

【联系方式】
手机号：____（必填，用于接收审核进度）
```

---

## 提高通过率的几点

**① 用途要具体**
别写"想更快点"。写清楚场景：
- 编译大型项目（如 Zig/LLVM 全量构建）
- 并行跑测试 / 批量处理文件
- CPU 推理（ONNX，无 GPU 环境）

**② 目标别写太高**
从 3 核提到 6 核是合理幅度。一次要 32 核大概率被拒，还可能进人工尽调。

**③ 处理周期**
官方说明 **1-2 个工作日**。配额类申请通常较快，部分产品在推荐范围内可自动通过。

---

## 提交后

工单号记下来，后续补充材料或催单都靠它。
审核期间沙盒照常能用，配额生效后需要**重开会话**（新容器才会带新规格）。

---

## 生效后要做的事

重开会话后跑一次：

```bash
bash boost_cpu.sh
```

脚本会自动检测 `nproc`，若已变为 6 就设成 600000，不用手改。
