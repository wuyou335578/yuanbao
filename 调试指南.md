# Zig + sokol-zig 调试与 bug 定位指南

> 2026-09-18 全部实测。环境：Zig 0.16.0 + sokol-zig(master) + C ABI。
> **结论：这套组合能真跑起来，也能快速定位运行时 bug。**

---

## 一、最重要的结论：sokol 自带"运行时验证层"

这是整套方案里**最实用的调试工具**，比 gdb 还管用。

sokol 在 Release 之外的构建里内置了完整的参数校验，
错误通过你注册的 log 回调输出，**带源文件、行号、错误 ID 和人类可读的原因**。

### 实测案例

故意写一个 bug（`gfx.makeBuffer(.{ .size = 0 })`），运行后输出：

```
[sg][error][id:186] sokol_gfx.h:23987:0:
    VALIDATE_BUFFERDESC_EXPECT_NONZERO_SIZE: sg_buffer_desc.size must be greater zero
[sg][error][id:190] sokol_gfx.h:24003:0:
    VALIDATE_BUFFERDESC_EXPECT_DATA: ...
[sg][panic][id:533] sokol_gfx.h:23960:0:
ABORTING because of [panic]
```

**它直接告诉你**：哪个文件、第几行、什么错误、为什么。
不需要 debugger，不需要看汇编。

### 怎么启用

```zig
gfx.setup(.{
    .environment = glue.environment(),
    .logger = .{ .func = sokol.log.func },   // ← 这一行是关键
});
```

**必须注册 logger**，否则错误静默发生，你只会看到一个莫名崩溃。

日志分级：`[warning]` / `[error]` / `[panic]`，可以直接 grep：

```bash
./myapp 2>&1 | grep -iE "\[error\]|\[panic\]"
```

---

## 二、能真实运行图形程序（Xvfb 虚拟屏）

没有显示器也能跑窗口程序：

```bash
apt-get install -y xvfb libgl1-mesa-dev libx11-dev libasound2-dev
Xvfb :99 -screen 0 1280x720x24 &
export DISPLAY=:99
export LIBGL_ALWAYS_SOFTWARE=1        # 强制软件渲染（llvmpipe）
./zig-out/bin/myapp
```

**实测结果**：sokol 窗口程序成功启动，输出 `[init] sokol 初始化完成`。

> ⚠️ `Xvfb` 要用 `setsid` 或 `start_new_session` 后台启动，
> 直接 `nohup ... &` 会让 shell 会话卡住。

---

## 三、内存问题：`zig test` + `std.testing.allocator`

Zig 标准库的测试分配器**自带内存检测**，不需要任何外部工具。

### 能抓什么（全部实测）

| 问题 | 输出 |
|---|---|
| 内存泄漏 | `[DebugAllocator] (err): memory address 0x... leaked:` + 精确行号 |
| 越界访问 | `panic: index out of bounds: index 8, len 8` + 行号 + 调用栈 |
| 重复释放 | 同上，带完整栈回溯 |

### 用法

```zig
const std = @import("std");

test "不泄漏" {
    const a = std.testing.allocator;
    const buf = try a.alloc(u8, 100);
    defer a.free(buf);       // 忘了这行，zig test 会报泄漏
}

test "不越界" {
    const a = std.testing.allocator;
    const buf = try a.alloc(u8, 8);
    defer a.free(buf);
    buf[8] = 1;              // 越界，zig test 直接 panic 并给出行号
}
```

```bash
zig test myfile.zig
```

**输出带完整调用栈**（精确到 `文件:行:列`），比 valgrind 的输出好读。

---

## 四、写代码时的实时诊断：zls

`zls`（Zig Language Server）**0.16.0 已就位**，19.2 MB。

- 补全、悬停、跳转定义
- **实时诊断**：保存时立刻标出类型错误、未定义标识符

> ⚠️ 版本必须与 zig 严格一致（都是 0.16.0），否则连不上。

配合之前的 `lsp_query.py`（在主仓库 `tools/`），
可以让 AI 直接查询代码真相，而不是猜 API。

---

## 五、Zig 自带的编译期检查

很多错误在编译时就被拦下，根本跑不到运行时：

```bash
$ zig build-obj c.zig
c.zig:2:16: error: use of undeclared identifier 'c'
```

Zig 的编译期检查很严：类型不匹配、未定义标识符、不可达代码、
未处理的 error 都会直接报错。**先让它编译通过，能过滤掉大半问题。**

### Sanitizer

```
-fsanitize-thread        线程数据竞争检测
-fsanitize-c[=mode]      C 代码的未定义行为检测（对 sokol 的 C ABI 很有用）
-fno-sanitize-*
```

使用方法（在 build.zig 里）：

```zig
mod.sanitize_thread = true;      // 或 .sanitize_c = true
```

> ⚠️ `-fsanitize=address` 这种 GCC 写法 Zig **不认**，
> 会报 `unrecognized parameter`。要用上面那两个。

---

## 六、实测不可用的工具（别浪费时间）

| 工具 | 状态 | 原因 |
|---|---|---|
| **gdb** | ❌ **用不了** | 进程陷在动态链接器里：`SIGSEGV at _dl_debug_initialize`，是沙盒环境本身的限制，不是程序 bug |
| valgrind | ❌ 未装 | 且 Zig 的 testing.allocator 已覆盖其功能 |
| ltrace / strace | ❌ 未装 | |

**gdb 跑不了不影响调试** —— sokol 的验证层 + zig test 的精确报错
已经覆盖了绝大部分场景，而且输出比 gdb 的栈回溯更好读。

---

## 七、推荐流程

```
1. 写代码          zls 实时诊断，边写边改
        ↓
2. zig build       编译期检查，过滤类型/语法错误
        ↓
3. zig test        testing.allocator 抓内存泄漏/越界
        ↓
4. Xvfb 里真跑     注册 sokol logger，error/panic 会带文件+行号
        ↓
5. 定位            按 sokol 给的 sokol_gfx.h:行号 直接查源码
```

## 八、本环境的额外坑（会打断调试）

### Zig 0.16 API 变动（调试时遇到编译报错先查这个）

```zig
// ❌ 0.16 移除了
b.addStaticLibrary(...)          → 用 b.addLibrary(.{ .linkage = .static })
exe.linkSystemLibrary("X11")     → 用 mod.linkSystemLibrary("X11", .{})
for (.{...}) |x|                 → 用 inline for (.{...}) |x|
```

### build.zig.zon 的 fingerprint

随便填会报错，Zig 会在错误信息里**给出正确的值**，照抄即可。

### sokol 必需的系统库

```
libx11-dev libgl1-mesa-dev libxi-dev libxcursor-dev
libxinerama-dev libxrandr-dev libasound2-dev
```
缺 X11 → `X11/Xlib.h file not found`；缺 ALSA → `alsa/asoundlib.h file not found`。

### C 实现的两个必需宏

```
-DIMPL                    不加则只有声明没有实现，链接缺符号
-DSOKOL_GLCORE            后端，Linux 桌面用这个
```

`SOKOL_DUMMY_BACKEND` 只能让 gfx 通过，
sokol_app 在 Linux 上仍会报 `unknown 3D API selected for Linux`。
